# Runtime environment — development defaults shown
# Copy to .env and adjust for your local setup

NODE_ENV=development
LOG_LEVEL=debug

# Coordinator
PORT=8080
COORDINATOR_ID=1

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dispatcher
DB_USER=postgres
DB_PASS=postgres
